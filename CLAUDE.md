# Claude adapter

Read `AGENTS.md` first, then read every nested `AGENTS.md` between the repository
root and the file being changed.  Those files are the canonical agent contract.
Do not infer project policy from local ignored `.claude/` files.

先读取根 `AGENTS.md`，再读取目标路径上的嵌套 `AGENTS.md`。不要在本文件复制
项目规则，也不要把本机忽略的 `.claude/` 内容当作仓库政策。
